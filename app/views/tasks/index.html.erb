<h1>My Tasks</h1>

  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task.id, task %>
      <%= task.content %>
    　  &gt; 
      <%= task.status %>
    </li>
    <% end %>
　</ul>

<%= link_to 'New Task', new_task_path, class: "btn btn-primary" %>

