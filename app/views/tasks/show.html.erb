<h1>id:<%= @task.id %>  ☜  Task details</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @task.id %></td>
  </tr>
  <tr>
    <th>Task</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>Status</th>
    <td><%= @task.status %></td>
  </tr>
</table>
    
 <%= link_to 'Back to My Tasks', tasks_path %>
 
 <%= link_to 'Edit My Task', edit_task_path(@task), class: 'btn btn-light'%>
 
 <%= link_to 'Delete My Task',@task, method: :delete, data: {confirm: '本当に削除してよろしいですか？'}, class: 'btn btn-danger'%>
</h1>

